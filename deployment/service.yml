kind: Service
apiVersion: v1
metadata:
  name: ice
  annotations:
    prometheus.io/probe: "true"
spec:
  selector:
    app: ice
    env: production
  ports:
  - name: web
    protocol: TCP
    port: 80
    targetPort: 8080
